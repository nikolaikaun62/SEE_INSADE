<Window x:Class="SEE_INSADE.UI.MainWindows.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="SEE INSADE - Professional Scanner"
        Height="900"
        Width="1600"
        WindowState="Maximized"
        Background="#0B0F14"
        FontFamily="Segoe UI">

    <Window.Resources>
        <SolidColorBrush x:Key="PageBrush" Color="#0B0F14"/>
        <SolidColorBrush x:Key="PanelBrush" Color="#121923"/>
        <SolidColorBrush x:Key="PanelAltBrush" Color="#182230"/>
        <SolidColorBrush x:Key="LineBrush" Color="#273447"/>
        <SolidColorBrush x:Key="TextBrush" Color="#E8EDF4"/>
        <SolidColorBrush x:Key="MutedTextBrush" Color="#8D99AA"/>
        <SolidColorBrush x:Key="AccentBrush" Color="#37D3B5"/>
        <SolidColorBrush x:Key="WarnBrush" Color="#F4B860"/>
        <SolidColorBrush x:Key="DangerBrush" Color="#EF6F6C"/>

        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
        </Style>

        <Style x:Key="TinyLabel" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource MutedTextBrush}"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="TextOptions.TextFormattingMode" Value="Display"/>
        </Style>

        <Style x:Key="MetricValue" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Margin" Value="0,2,0,0"/>
        </Style>

        <Style x:Key="ControlGroupStyle" TargetType="GroupBox">
            <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="Margin" Value="0,0,0,20"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="GroupBox">
                        <StackPanel>
                            <TextBlock Text="{TemplateBinding Header}"
                                       Foreground="{StaticResource MutedTextBrush}"
                                       FontSize="11"
                                       FontWeight="Bold"
                                       Margin="0,0,0,10"/>
                            <ContentPresenter/>
                        </StackPanel>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ModernButton" TargetType="Button">
            <Setter Property="Background" Value="#182230"/>
            <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
            <Setter Property="BorderBrush" Value="#2D3A4D"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="12,9"/>
            <Setter Property="Margin" Value="4"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="MinHeight" Value="38"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Root"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"
                                              Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Root" Property="Background" Value="#223047"/>
                                <Setter TargetName="Root" Property="BorderBrush" Value="#37D3B5"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Root" Property="Background" Value="#0F766E"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="Slider">
            <Setter Property="Foreground" Value="{StaticResource AccentBrush}"/>
            <Setter Property="Margin" Value="0,6,0,0"/>
        </Style>

        <Style TargetType="CheckBox">
            <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
            <Setter Property="Margin" Value="0,7,0,0"/>
            <Setter Property="FontSize" Value="12"/>
        </Style>

        <Style TargetType="TabControl">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
        </Style>

        <Style TargetType="TabItem">
            <Setter Property="Foreground" Value="{StaticResource MutedTextBrush}"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Padding" Value="14,9"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
        </Style>

        <Style x:Key="MetricTile" TargetType="Border">
            <Setter Property="Background" Value="#111821"/>
            <Setter Property="BorderBrush" Value="{StaticResource LineBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="6"/>
            <Setter Property="Padding" Value="12"/>
            <Setter Property="Margin" Value="0,0,10,0"/>
        </Style>
    </Window.Resources>

    <Grid Background="{StaticResource PageBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="72"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="34"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="#0E141C" BorderBrush="{StaticResource LineBrush}" BorderThickness="0,0,0,1">
            <Grid Margin="22,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" VerticalAlignment="Center">
                    <TextBlock Text="SEE INSADE" FontSize="22" FontWeight="Bold"/>
                    <TextBlock Text="X-RAY LINE SCAN EMULATOR" Style="{StaticResource TinyLabel}" Margin="1,2,0,0"/>
                </StackPanel>

                <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                    <Border Background="#11251F" BorderBrush="#245A4D" BorderThickness="1" CornerRadius="6" Padding="12,7">
                        <StackPanel Orientation="Horizontal">
                            <Ellipse Width="8" Height="8" Fill="{StaticResource AccentBrush}" Margin="0,0,8,0"/>
                            <TextBlock x:Name="StatusText" Text="READY" Foreground="{StaticResource AccentBrush}" FontWeight="Bold"/>
                        </StackPanel>
                    </Border>
                </StackPanel>

                <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                    <Button Content="Settings" Style="{StaticResource ModernButton}" Click="OpenSettings_Click"/>
                    <Button Content="Diagnostics" Style="{StaticResource ModernButton}" Click="OpenDiagnostics_Click"/>
                    <Button Content="Calibrate" Style="{StaticResource ModernButton}" Click="OpenCalibration_Click"/>
                    <Button Content="Detectors" Style="{StaticResource ModernButton}" Click="OpenDetectorVisualization_Click"/>
                </StackPanel>
            </Grid>
        </Border>

        <Grid Grid.Row="1" Margin="18">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="380"/>
            </Grid.ColumnDefinitions>

            <Grid Grid.Column="0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <UniformGrid Grid.Row="0" Columns="5" Margin="0,0,0,14">
                    <Border Style="{StaticResource MetricTile}">
                        <StackPanel>
                            <TextBlock Text="LINE POSITION" Style="{StaticResource TinyLabel}"/>
                            <TextBlock x:Name="PositionText" Text="Belt: 0px  Column: 0" Style="{StaticResource MetricValue}"/>
                        </StackPanel>
                    </Border>
                    <Border Style="{StaticResource MetricTile}">
                        <StackPanel>
                            <TextBlock Text="OBJECTS" Style="{StaticResource TinyLabel}"/>
                            <TextBlock x:Name="ObjectsText" Text="Objects: 0" Style="{StaticResource MetricValue}"/>
                        </StackPanel>
                    </Border>
                    <Border Style="{StaticResource MetricTile}">
                        <StackPanel>
                            <TextBlock Text="CONVEYOR" Style="{StaticResource TinyLabel}"/>
                            <TextBlock x:Name="SpeedText" Text="Speed: 1.0x" Style="{StaticResource MetricValue}"/>
                        </StackPanel>
                    </Border>
                    <Border Style="{StaticResource MetricTile}">
                        <StackPanel>
                            <TextBlock Text="DETECTOR ARRAY" Style="{StaticResource TinyLabel}"/>
                            <TextBlock x:Name="DetectorsText" Text="Detectors: 400/400" Style="{StaticResource MetricValue}"/>
                        </StackPanel>
                    </Border>
                    <Border Style="{StaticResource MetricTile}" Margin="0">
                        <StackPanel>
                            <TextBlock Text="MATERIALS" Style="{StaticResource TinyLabel}"/>
                            <TextBlock x:Name="MaterialsText" Text="Materials: 0 types" Style="{StaticResource MetricValue}"/>
                        </StackPanel>
                    </Border>
                </UniformGrid>

                <Border Grid.Row="1"
                        Background="#070A0E"
                        BorderBrush="{StaticResource LineBrush}"
                        BorderThickness="1"
                        CornerRadius="8"
                        Padding="12">
                    <Grid>
                        <TabControl x:Name="ViewTabControl">
                            <TabItem Header="STANDARD">
                                <Grid Background="#020304">
                                    <Viewbox Stretch="Uniform">
                                        <Image x:Name="ScanImage" Stretch="None" RenderOptions.BitmapScalingMode="NearestNeighbor"/>
                                    </Viewbox>
                                    <Border HorizontalAlignment="Right" Width="2" Background="{StaticResource AccentBrush}" Opacity="0.9"/>
                                    <Border HorizontalAlignment="Right" Width="34" Background="#1837D3B5" IsHitTestVisible="False"/>
                                </Grid>
                            </TabItem>

                            <TabItem Header="MATERIAL">
                                <Grid Background="#020304">
                                    <Viewbox Stretch="Uniform">
                                        <Image x:Name="MaterialImage" Stretch="None" RenderOptions.BitmapScalingMode="NearestNeighbor"/>
                                    </Viewbox>
                                    <Border HorizontalAlignment="Right" Width="2" Background="{StaticResource AccentBrush}" Opacity="0.9"/>
                                </Grid>
                            </TabItem>

                            <TabItem Header="DENSITY">
                                <Grid Background="#020304">
                                    <Viewbox Stretch="Uniform">
                                        <Image x:Name="DensityImage" Stretch="None" RenderOptions.BitmapScalingMode="NearestNeighbor"/>
                                    </Viewbox>
                                    <Border HorizontalAlignment="Right" Width="2" Background="{StaticResource AccentBrush}" Opacity="0.9"/>
                                </Grid>
                            </TabItem>

                            <TabItem Header="FILTERED">
                                <Grid Background="#020304">
                                    <Viewbox Stretch="Uniform">
                                        <Image x:Name="FilteredImage" Stretch="None" RenderOptions.BitmapScalingMode="NearestNeighbor"/>
                                    </Viewbox>
                                    <Border HorizontalAlignment="Right" Width="2" Background="{StaticResource AccentBrush}" Opacity="0.9"/>
                                </Grid>
                            </TabItem>
                        </TabControl>
                    </Grid>
                </Border>
            </Grid>

            <Border Grid.Column="1"
                    Background="{StaticResource PanelBrush}"
                    BorderBrush="{StaticResource LineBrush}"
                    BorderThickness="1"
                    CornerRadius="8"
                    Margin="16,0,0,0">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel Margin="18">
                        <TextBlock Text="Acquisition Control" FontSize="18" FontWeight="Bold" Margin="0,0,0,18"/>

                        <GroupBox Header="SCAN CONTROLS" Style="{StaticResource ControlGroupStyle}">
                            <StackPanel>
                                <TextBlock Text="Conveyor speed" Style="{StaticResource TinyLabel}"/>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="54"/>
                                    </Grid.ColumnDefinitions>
                                    <Slider x:Name="SpeedSlider" Minimum="0.1" Maximum="3.0" Value="1.0"/>
                                    <TextBlock x:Name="SpeedValueText" Grid.Column="1" Text="1.0x" HorizontalAlignment="Right" VerticalAlignment="Center" Foreground="{StaticResource AccentBrush}" FontWeight="Bold"/>
                                </Grid>

                                <TextBlock Text="Detector sensitivity" Style="{StaticResource TinyLabel}" Margin="0,14,0,0"/>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="54"/>
                                    </Grid.ColumnDefinitions>
                                    <Slider x:Name="SensitivitySlider" Minimum="0.1" Maximum="2.0" Value="1.0"/>
                                    <TextBlock x:Name="SensitivityValueText" Grid.Column="1" Text="100%" HorizontalAlignment="Right" VerticalAlignment="Center" Foreground="{StaticResource AccentBrush}" FontWeight="Bold"/>
                                </Grid>

                                <UniformGrid Columns="2" Margin="0,16,0,0">
                                    <Button Content="Start" Style="{StaticResource ModernButton}" Click="StartScan_Click"/>
                                    <Button Content="Pause" Style="{StaticResource ModernButton}" Click="PauseScan_Click"/>
                                    <Button Content="Stop" Style="{StaticResource ModernButton}" Click="StopScan_Click"/>
                                    <Button Content="Reset" Style="{StaticResource ModernButton}" Click="ResetScan_Click"/>
                                    <Button Content="Snapshot" Style="{StaticResource ModernButton}" Click="TakeSnapshot_Click"/>
                                    <Button Content="Analyze" Style="{StaticResource ModernButton}" Click="OpenAnalysis_Click"/>
                                </UniformGrid>
                            </StackPanel>
                        </GroupBox>

                        <GroupBox Header="PLUGINS" Style="{StaticResource ControlGroupStyle}">
                            <StackPanel>
                                <ComboBox x:Name="PluginComboBox"
                                          MinHeight="36"
                                          Background="{StaticResource PanelAltBrush}"
                                          Foreground="{StaticResource TextBrush}"
                                          BorderBrush="{StaticResource LineBrush}"/>
                                <Button Content="Open Plugin" Style="{StaticResource ModernButton}" Margin="0,10,0,0" Click="LaunchPlugin_Click"/>
                            </StackPanel>
                        </GroupBox>

                        <GroupBox Header="IMAGE FILTERS" Style="{StaticResource ControlGroupStyle}">
                            <StackPanel>
                                <CheckBox x:Name="BrightnessFilterCheck" Content="Brightness" Checked="Filter_Changed" Unchecked="Filter_Changed"/>
                                <Slider x:Name="BrightnessSlider" Minimum="0.1" Maximum="3.0" Value="1.0" ValueChanged="Filter_Changed"/>

                                <CheckBox x:Name="ContrastFilterCheck" Content="Contrast" Checked="Filter_Changed" Unchecked="Filter_Changed"/>
                                <Slider x:Name="ContrastSlider" Minimum="0.1" Maximum="3.0" Value="1.0" ValueChanged="Filter_Changed"/>

                                <CheckBox x:Name="MaterialFilterCheck" Content="Material enhancement" Checked="Filter_Changed" Unchecked="Filter_Changed"/>
                                <CheckBox x:Name="EdgeFilterCheck" Content="Edge detection" Checked="Filter_Changed" Unchecked="Filter_Changed"/>
                                <CheckBox x:Name="NoiseFilterCheck" Content="Noise reduction" Checked="Filter_Changed" Unchecked="Filter_Changed"/>

                                <Button Content="Apply Filters" Style="{StaticResource ModernButton}" Margin="0,14,0,0" Click="ApplyFilters_Click"/>
                            </StackPanel>
                        </GroupBox>

                        <GroupBox Header="DETECTOR STATUS" Style="{StaticResource ControlGroupStyle}">
                            <StackPanel>
                                <ProgressBar x:Name="DetectorHealthBar" Value="95" Maximum="100" Height="12" Margin="0,0,0,8"/>
                                <TextBlock x:Name="DetectorHealthText" Text="Health: 95%" Foreground="{StaticResource AccentBrush}" FontSize="12" FontWeight="SemiBold"/>
                                <UniformGrid Columns="2" Margin="0,12,0,0">
                                    <Button Content="Test Array" Style="{StaticResource ModernButton}" Click="TestDetectors_Click"/>
                                    <Button Content="Calibrate" Style="{StaticResource ModernButton}" Click="CalibrateDetectors_Click"/>
                                </UniformGrid>
                            </StackPanel>
                        </GroupBox>

                        <Border Background="{StaticResource PanelAltBrush}" BorderBrush="{StaticResource LineBrush}" BorderThickness="1" CornerRadius="6" Padding="14">
                            <StackPanel>
                                <TextBlock Text="SYSTEM INFO" Style="{StaticResource TinyLabel}" Margin="0,0,0,8"/>
                                <TextBlock x:Name="FrameRateText" Text="FPS: 0" FontSize="12"/>
                                <TextBlock x:Name="MemoryText" Text="Memory: 0 MB" FontSize="12" Margin="0,4,0,0"/>
                                <TextBlock x:Name="ProcessingText" Text="Processing: Idle" FontSize="12" Margin="0,4,0,0"/>
                                <TextBlock x:Name="FilterCountText" Text="Active Filters: 0" FontSize="12" Margin="0,4,0,0"/>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </ScrollViewer>
            </Border>
        </Grid>

        <StatusBar Grid.Row="2" Background="#0E141C" BorderBrush="{StaticResource LineBrush}" BorderThickness="0,1,0,0">
            <StatusBarItem>
                <TextBlock x:Name="MainStatusText" Text="SEE INSADE - Ready for scanning" Foreground="{StaticResource TextBrush}" FontWeight="SemiBold"/>
            </StatusBarItem>
            <StatusBarItem HorizontalAlignment="Right">
                <TextBlock x:Name="TimeText" Text="00:00:00" Foreground="{StaticResource MutedTextBrush}" Margin="10,0,0,0"/>
            </StatusBarItem>
        </StatusBar>
    </Grid>
</Window>
